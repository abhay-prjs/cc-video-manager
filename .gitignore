# Secrets & credentials
config.json
config.json.save
config.json.save.*
credentials.json
token.json

# Runtime state files
discord_queue.json
pending_assignments.json
pending_folders.json
pending_reviews.json
assignment_messages.json
watched_files.json
folder_update_msgs.json
edited_files.json
clients.json
watch_channel.json
page_token.json

# Logs
*.log
logs/
watch_renewal.log

# Backups
*.bak-*

# Python
__pycache__/
*.pyc
*.pyo

# Misc
*.deb
dashboard_ref.png
pending_dashboard_pushes.json
dashboard_batches.json

# Claude Code local settings — machine-specific, has accumulated secrets in allowed-command strings
.claude/settings.local.json

# One-off scripts and manual diagnostics. Written to answer one question, run
# once, never imported by a service. See test/README.md — anything genuinely
# worth re-running graduates to the repo root next to the services instead.
test/*
!test/README.md

# Local dev
.venv/
.DS_Store
